import React from "react"
import { States } from "../../../../types/Types"

interface IProps {
    state: States;
}

export const State = ({state}: IProps) => {
    return (
        <div>
            
        </div>
    )
}