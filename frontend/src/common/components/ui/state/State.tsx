import React from "react"
import { States } from "../../../../types/States"

interface IProps {
    state: States;
}

export const State = ({state}: IProps) => {
    return (
        <div>
            
        </div>
    )
}